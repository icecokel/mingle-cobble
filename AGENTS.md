# icecoke-cobblemon Workspace Rules

이 워크스페이스는 `icenux-ms7b23`에서 실행 중인 개인용 Cobblemon 서버 `icecoke-cobblemon`의 운영 문서, 서버 보조 스크립트, 로컬 접속 조건을 관리한다. 현재 기준 문서는 `docs/server/`이다.

현재 서버는 버전 라인으로 구분한다.

- `icecoke-cobblemon-173`: 현재 승격 운영 라인. 포트 `25566`, 경로 `/home/icenux/minecraft/icecoke-cobblemon-173-test`, Minecraft `1.21.1`, Fabric server loader `0.17.3`, Cobblemon `1.7.3`.
- `icecoke-cobblemon-161`: 보존용 레거시 라인. 포트 `25565`, 경로 `/home/icenux/minecraft/mingle-lounge`, Minecraft `1.21.1`, Fabric `0.16.14`, Cobblemon `1.6.1`.

경로의 `173-test`는 과거 테스트 인스턴스에서 승격된 흔적이며, 현재 표시명과 운영 명칭은 `icecoke-cobblemon-173`이다. 파일 이동 없이 쓰는 것을 안정성 기준으로 삼는다.

## 판단 우선순위

1. 안정성을 최우선으로 둔다.
   - 서버 접속 성공과 플레이 지속 가능성이 새 기능보다 중요하다.
   - 모드 추가는 마지막 선택지로 본다.
   - 기능이나 역할이 겹치는 모드는 피한다.
   - 현재 샘플팩 기반으로 검증된 서버/클라이언트 구성을 기준선으로 삼는다.

2. 현재 서버 세팅을 먼저 확인한다.
   - `docs/server/README.md`와 `docs/server/SERVER_CONCEPT.md`를 운영 기준 문서로 본다.
   - 기본 작업 기준은 `icecoke-cobblemon-173` 라인이다.
   - `icecoke-cobblemon-161` 라인은 레거시 보존과 비교 기준으로 본다.
   - 기존 `cobblemon-current-settings.md`는 레거시 참고 자료다. 현재 서버 라인 판단은 `docs/server/README.md`, `SERVER_CONCEPT.md`, `COBBLEMON_173_LINE.md`를 우선한다.
   - 서버와 로컬 클라이언트가 같은 주요 모드를 요구하는지 확인한다.

3. 목적을 작게 정의한다.
   - 사용자가 원하는 것이 "상점", "수급 완화", "편의 기능", "버그 수정" 중 무엇인지 구분한다.
   - 목적이 아이템 수급이면 경제 모드보다 거래, 보상 설정, 데이터팩, 기존 모드 설정을 먼저 검토한다.
   - 목적이 플레이 안정성이면 새 콘텐츠보다 충돌 제거와 최소 구성을 우선한다.

4. 기존 기능과 설정으로 해결할 수 있는지 먼저 본다.
   - 바닐라 명령어, gamerule, 데이터팩, 기존 Cobblemon/Cobbleloots 설정을 먼저 검토한다.
   - 새 모드가 필요한 경우, 서버 전용인지 클라이언트도 필요한지 구분한다.
   - 새 서버 환경에서 설치 가능한지, 버전이 맞는지, 의존성이 늘어나는지 확인한다.

5. 변경 범위를 작게 유지한다.
   - 문서 수정, 설정 변경, 모드 추가를 한 번에 섞지 않는다.
   - 서버 설정을 바꿀 때는 로컬 접속 조건이 바뀌는지 함께 판단한다.
   - 클라이언트 모드가 바뀌면 접속 가이드도 갱신 대상이다.
   - 현재 워크스페이스는 문서와 스크립트 중심으로 유지한다.
   - 기준을 다시 잡기 전까지 `client-mods/`, `datapacks/`, `resourcepacks/`, `sample/` 같은 산출물은 Git 보관 대상에서 제외한다.
   - 클라이언트 패치나 데이터팩 산출물이 다시 필요하면, 먼저 보관 위치와 Git 포함 여부를 문서로 정한 뒤 작업한다.

6. 조사와 작업을 명확히 분리한다.
   - 사용자가 "조사만"이라고 하면 파일, 서버, 브라우저 설정을 변경하지 않는다.
   - 브라우저 확인이 필요할 때는 Chrome 확장자 기능을 활용한다.
   - 조사 결과는 가능 여부, 리스크, 추천 순서로 정리한다.
   - 작업을 시작하기 전에는 어떤 파일이나 설정을 바꿀지 먼저 말한다.

7. 문서는 접속 가능한 상태를 목표로 쓴다.
   - 현재 운영 문서는 `docs/server/`에 둔다.
   - `docs/server/MODS_AND_CLIENT.md`는 현재 로컬 Modrinth 프로필과 서버 접속 조건을 설명해야 한다.
   - `docs/server/OPS_RUNBOOK.md`는 에이전트나 관리자가 현재 서버를 점검, 백업, 복구할 수 있어야 한다.
   - 설치와 실행에 직접 필요 없는 설명은 줄인다.
   - 뉴비 플레이 팁은 `cobblemon-newbie-guide.md`에 둔다.
   - 운영 메모와 현재 상태는 `docs/server/SERVER_CONCEPT.md`, `docs/server/MODS_AND_CLIENT.md`, `docs/server/CHANGELOG.md`에 둔다.
   - 모드 jar, 데이터팩 zip, 리소스팩 zip, 샘플 zip 원본은 워크스페이스에 상시 보관하지 않는다.
   - 서버 작업을 했으면 `docs/server/CHANGELOG.md`에 `요청`, `목표`, `작업내용`, `기대작용`, `특이사항`을 남긴다.
   - 필요하면 `검증`, `백업`, `재시작 필요 여부`, `클라이언트 영향`도 같은 기록에 포함한다.

8. 검증 없이 완료라고 말하지 않는다.
   - 서버/로컬 실행을 건드린 경우 가능한 범위에서 접속 조건을 확인한다.
   - 사용자가 로컬 테스트를 요청하면 테스트 종료 후 Minecraft를 닫는다.
   - 직접 검증하지 못한 부분은 "확인 필요"로 남긴다.

9. 서버 재시작은 명시 요청이 있을 때만 처리한다.
   - 현재 운영은 SSH, `screen`, 서버 콘솔 명령을 사용할 수 있는 방식이다.
   - 173 라인은 `/home/icenux/minecraft/icecoke-cobblemon-173-test`의 `icecoke-173` screen 세션을 기준으로 점검한다.
   - 사용자가 재시작을 명시하지 않았으면 설정 변경 후에도 재시작하지 않고, 재시작 필요 여부만 말한다.
   - `.mca`, `level.dat`, `playerdata` 같은 월드 파일 교체는 업로드 후 재다운로드 검증과 인게임 확인 전까지 완료로 말하지 않는다.
   - 서버 재시작이 필요한 작업 후에는 사용자가 재시작을 요청해야 실제 반영된다고 명확히 말한다.
   - 세부 운영 절차는 `docs/server/OPS_RUNBOOK.md`를 우선하고, `server-operation-constraints.md`는 File Browser 기반 레거시 제약 참고용으로 본다.

10. 서버 작업 기록은 구조화한다.
   - 기록 위치는 기본적으로 `docs/server/CHANGELOG.md`다.
   - 기록 항목은 `요청`, `목표`, `작업내용`, `기대작용`, `특이사항`을 기본으로 한다.
   - 서버 파일, 설정, 모드, 데이터팩, 월드, 재시작, 접속 조건, 운영 문서를 바꾼 경우 모두 기록 대상이다.
   - 작업 중 계획과 실제 결과가 달라졌으면 최종 기록에는 실제 수행 내용과 남은 확인 필요 사항을 쓴다.

## 추천 판단 흐름

1. 현재 문서와 설치 모드 목록을 확인한다.
2. 사용자의 실제 목적을 한 문장으로 좁힌다.
3. 기존 설정으로 가능한 해결책을 먼저 찾는다.
4. 새 모드가 필요한지 판단하고, 필요한 경우 버전/의존성/서버 환경/클라이언트 영향도를 본다.
5. 안정성 기준으로 추천안을 정한다.
6. 작업 요청이면 변경 범위를 말한 뒤 실행한다.
7. 서버 재구축이나 복구 작업이면 `docs/server/OPS_RUNBOOK.md`와 관련 세부 문서 절차에 맞는지 확인한다.
8. 서버 재시작이 필요한 변경이면 사용자 버튼 재시작 필요 여부를 말한다.
9. 변경 후 관련 문서, 구조화된 작업 기록, 검증 결과를 함께 정리한다.

## 기본 결론 성향

- "쉽게 만들자"는 요청은 먼저 Cobbleloots, 거래, 보상, 레시피 같은 가벼운 조정으로 본다.
- "새 기능을 넣자"는 요청은 현재 모드와 기능 중복 여부부터 본다.
- "안 된다"는 요청은 추측보다 현재 설정, 로그, 버전, 클라이언트 모드 목록을 확인한다.
- 안정성과 편의성이 충돌하면 안정성을 우선하고, 편의성은 작은 변경으로 보완한다.
