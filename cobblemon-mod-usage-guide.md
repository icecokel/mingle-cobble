# Mingle Lounge Cobblemon 모드 사용 가이드

마지막 갱신: 2026-05-23

이 문서는 서버에 접속한 뒤 자주 쓰는 Cobblemon 조작, 지도 모드, 편의 기능을 정리합니다. 설치 방법은 [접속 가이드](/Users/smlee/mingle-lounge/cobblemon-client-setup-guide.md), 초반 플레이 흐름은 [뉴비 가이드](/Users/smlee/mingle-lounge/cobblemon-newbie-guide.md)를 봅니다.

키 설정은 현재 로컬 Modrinth 프로필 기준입니다. 개인 설정이나 키 충돌로 다를 수 있으니, 안 맞으면 Minecraft의 `Options` -> `Controls`에서 `Cobblemon`, `Xaero`, `Battle Extras`를 검색해 확인합니다.

## 1. Cobblemon 기본 조작

| 행동 | 키 | 설명 |
| --- | --- | --- |
| 포켓몬 꺼내기/넣기 | `R` | 왼쪽 파티 UI에서 선택된 포켓몬을 밖으로 꺼내거나 다시 넣습니다. |
| 포켓몬 요약/메뉴 | `M` | 선택한 포켓몬의 요약 화면과 관련 메뉴를 엽니다. |
| 파티 선택 위/아래 | `Up` / `Down` | 왼쪽 파티 UI에서 선택 포켓몬을 바꿉니다. |
| 파티 UI 숨김 | `O` | 화면 왼쪽의 파티 UI를 숨기거나 다시 표시합니다. |
| 라이딩 자유 시점 | `Left Alt` | 라이딩 중 시점이 불편할 때 자유 시점으로 봅니다. |

전투에 먼저 내보낼 포켓몬은 왼쪽 파티 UI에서 선택된 포켓몬입니다. 원하는 포켓몬이 먼저 나가지 않으면 `Up` / `Down`으로 선택을 바꾼 뒤 `R`을 누릅니다.

## 2. 전투와 로그

| 행동 | 키 | 설명 |
| --- | --- | --- |
| 전투 로그 열기 | `'` | Cobblemon Battle Extras의 전투 로그를 확인합니다. |

현재 로컬 설정에서는 전투 로그 키가 작은따옴표(`'`)입니다. 기존에 `B`로 알고 있던 경우도 있을 수 있으니, 키가 먹지 않으면 조작 설정에서 `Battle Extras`를 검색합니다.

전투 속도는 서버와 로컬 모두 빠르게 조정되어 있습니다. 전투가 너무 빨리 지나간다고 느껴지면 로그를 열어 어떤 기술이 들어갔는지 확인합니다.

## 3. 지도 모드

지도 모드는 선택 모드입니다. 없어도 서버 접속은 되지만, 마을, 포켓몬센터, 집 위치를 기억하기 쉬워집니다.

현재 로컬 프로필 기준 지도 모드:

```text
Xaero's Minimap
Xaero's World Map
```

주요 단축키:

| 행동 | 키 | 설명 |
| --- | --- | --- |
| 미니맵 설정 | `Y` | 미니맵 표시 방식, 크기, 위치 등을 조정합니다. |
| 전체 월드맵 열기 | `,` | 지금까지 탐험한 지역을 큰 지도로 봅니다. |
| 월드맵 설정 | `]` | 월드맵 관련 설정을 엽니다. |
| 미니맵 크게 보기 | `Z` | 미니맵을 확대해서 주변 지형을 봅니다. |
| 웨이포인트 목록 | `U` | 저장한 위치 목록을 봅니다. |
| 새 웨이포인트 추가 | `.` | 현재 위치를 저장합니다. |
| 즉시 웨이포인트 추가 | `Numpad +` | 현재 위치를 빠르게 저장합니다. |

처음에는 아래 위치만 저장해도 충분합니다.

1. 처음 스폰된 마을
2. 자주 쓰는 포켓몬센터
3. 집이나 창고 위치
4. 규토리 농장이나 자주 가는 채굴 장소

## 4. 레시피 검색

프로필에 레시피 검색 모드가 들어 있으면 인벤토리 화면 오른쪽에 아이템 목록이나 검색창이 보입니다. 제작법이 헷갈릴 때는 먼저 검색창에 아래처럼 입력합니다.

```text
pokeball
great ball
ultra ball
apricorn
```

한국어 이름으로 안 나오면 영어 이름으로 검색합니다. Cobblemon 아이템은 한국어 번역과 게임 내 표시명이 다를 수 있어, `규토리`가 안 보이면 `apricorn`으로 찾는 편이 빠릅니다.

## 5. 서버 적용 모드가 하는 일

| 모드 | 플레이어가 체감하는 기능 |
| --- | --- |
| `Cobblemon` | 포켓몬 스폰, 전투, 포획, 파티 UI, PC, 회복 장치, 기본 라이딩 |
| `Cobbleloots` | 필드에 루트볼/보상볼이 생기고, 보상으로 몬스터볼과 회복템을 얻을 수 있음 |
| `Cobblemon Spawn Alerts` | 희귀하거나 중요한 포켓몬 스폰 알림을 표시 |
| `Capture XP` | 포켓몬을 잡을 때도 경험치를 얻음 |
| `Cobblemon Battle Extras` | 전투 로그 같은 전투 보조 기능 |
| `Cobblemon Pokemon Badges` | 관장을 이겼을 때 받는 관동 배지 아이템을 추가 |
| `Mega Showdown` | 메가진화 관련 콘텐츠 |
| `Myths and Legends` | 특정 키 아이템과 조건 기반 전설/환상 포켓몬 콘텐츠 |
| `Limited Legends` | 플레이어별 전설/환상 포켓몬 소지 제한 |

의존성 모드는 직접 조작하는 기능이 아니라 다른 모드가 정상 작동하기 위해 필요한 모드입니다. 예를 들어 `Fabric API`, `Architectury`, `Accessories`, `Tim Core` 같은 모드는 설치되어 있어야 하지만 뉴비가 따로 사용할 메뉴는 거의 없습니다.

## 6. 막히면 확인할 것

키가 안 먹거나 화면이 다르면 아래 순서로 확인합니다.

1. `Options` -> `Controls`에서 모드 이름으로 검색합니다.
2. 같은 키에 여러 기능이 겹쳐 있는지 확인합니다.
3. 지도 모드가 없다면 접속에는 문제가 없지만 지도 단축키는 작동하지 않습니다.
4. 레시피 검색창이 안 보이면 해당 프로필에 레시피 검색 모드가 없을 수 있습니다.
5. 접속 자체가 안 되면 [접속 가이드](/Users/smlee/mingle-lounge/cobblemon-client-setup-guide.md)의 필수 모드 목록을 먼저 확인합니다.
