# Mingle Lounge 서버 운영 제약

마지막 갱신: 2026-06-03

> 현재 `icecoke-cobblemon` 서버는 SSH, `screen`, 서버 콘솔 명령을 사용할 수 있는 운영 방식으로 바뀌었습니다. 이 문서는 File Browser 기반 이전 운영 제약 참고용입니다. 현재 운영 제약과 점검 절차는 [docs/server/OPS_RUNBOOK.md](/Users/smlee/mingle-lounge/docs/server/OPS_RUNBOOK.md)를 우선합니다.

이 문서는 File Browser 기반 Mingle Lounge 서버 작업을 할 때 반드시 전제로 둬야 하는 운영 제약을 정리합니다. 서버 세팅 자체는 [기준 세팅](/Users/smlee/mingle-lounge/cobblemon-current-settings.md)을 기준으로 봅니다.

## 핵심 전제

현재 관리자는 서버를 직접 정지할 수 없습니다.

가능한 작업:

- File Browser에서 서버 파일 읽기, 다운로드, 업로드, 덮어쓰기
- 서버 관리 화면에서 서버 재시작
- 서버 접속 후 관리자 명령어 실행

불가능하거나 현재 권한으로 확인되지 않은 작업:

- 서버를 완전히 정지한 상태로 파일 교체
- File Browser 또는 서버 관리 화면에서 서버 터미널/셸 사용
- 서버 프로세스를 터미널에서 직접 제어
- 서버 파일이 열려 있지 않은 상태를 보장한 뒤 월드 파일 교체

File Browser의 파일 관리 권한과 서버 터미널 권한은 별개로 본다. 현재 운영 전제에서는
File Browser로 파일을 수정할 수는 있지만, 서버 터미널에서 명령을 실행하는 방식은 사용할
수 없다. 서버 명령이 필요한 작업은 인게임 OP 명령, 서버 관리 화면에 준비된 고정 버튼,
또는 재시작 후 반영되는 파일/데이터팩 변경으로 설계한다.

## 작업 원칙

서버 정지가 필요한 작업은 기본적으로 고위험 작업으로 본다.

서버 파일 변경의 표준 방식은 로컬에서 수정본을 만든 뒤 File Browser로 서버에 업로드하는
것이다. 서버 터미널에서 직접 편집하거나 명령으로 파일을 생성하는 방식은 사용하지 않는다.
작업 전에는 서버의 현재 파일을 내려받아 기준본으로 삼고, 작업 후에는 업로드한 파일을 다시
다운로드해 내용이 맞는지 확인한다.

특히 아래 파일은 서버가 실행 중이면 메모리 상태와 디스크 상태가 어긋날 수 있다.

```text
world/region/*.mca
world/entities/*.mca
world/poi/*.mca
world/playerdata/*.dat
world/level.dat
```

이 파일들을 수정하거나 업로드할 때는 "적용 완료"라고 단정하지 말고, 업로드 후 재시작과 재검증이 필요하다고 기록한다.

## 재시작만 가능한 환경에서의 안전한 순서

설정 파일, 데이터팩, 모드처럼 서버 재시작으로 반영되는 파일은 다음 순서로 진행한다.

1. 현재 파일을 백업하거나 로컬에 내려받는다.
2. 로컬에서 수정본을 만든다.
3. 로컬에서 파일 구조, 문법, 버전 호환성을 확인한다.
4. File Browser로 정확한 서버 경로에 업로드한다.
5. 업로드한 파일을 다시 다운로드해 로컬 수정본과 비교한다.
6. 사용자가 서버 관리 화면에서 재시작한다.
7. 서버 기동 후 로그, 명령어, 접속 테스트로 확인한다.

월드 청크 복구처럼 `.mca` 파일을 직접 교체하는 작업은 다음을 추가로 지킨다.

1. 대상 청크 좌표와 region 파일명을 명확히 계산한다.
2. `world/region`, `world/entities`, `world/poi` 세 파일을 함께 준비한다.
3. 업로드 후 같은 파일을 다시 다운로드해서 해시 또는 청크 헤더를 비교한다.
4. 재시작 후 인게임에서 해당 청크가 실제로 재생성됐는지 확인한다.
5. 확인 전에는 성공으로 기록하지 않는다.

## 청크 초기화 작업의 현재 리스크

서버를 완전히 정지할 수 없으므로 `.mca` 교체 방식의 청크 초기화는 결과가 보장되지 않는다.

가능한 실패 원인:

- 서버가 실행 중인 월드 데이터를 메모리에 유지하고 있음
- 업로드한 `.mca`가 실제 운영 파일에 반영되지 않음
- 손상된 건물이 여러 청크에 걸쳐 있어 초기화 범위가 부족함
- 재시작 후 기존 청크 상태가 다시 저장됨

따라서 청크 초기화는 "업로드 완료"와 "복구 완료"를 분리해서 말한다. 복구 완료는 서버 재시작 후 인게임 확인까지 끝났을 때만 사용한다.

## 권장 대안

서버 정지가 불가능한 상황에서는 큰 월드 파일 교체보다 작은 인게임 명령어 복구를 우선한다.

권장 순서:

1. 구조물 일부 손상은 `/fill`, `/clone`, `/place template` 같은 명령어로 직접 복구한다.
2. 스폰 위치, 보상, 몹 스폰, 데이터팩 같은 설정은 파일 수정 후 재시작으로 반영한다.
3. 청크 초기화는 백업, 업로드 검증, 재시작, 인게임 검증까지 감당할 수 있을 때만 진행한다.
